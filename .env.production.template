# 🌐 Production Environment Configuration
# Copy this to .env.production and customize for your deployment

# =====================================
# 🔑 API Keys & Secrets (REQUIRED)
# =====================================

# OpenAI API Key - Get from https://platform.openai.com/account/api-keys
OPENAI_API_KEY=your_production_openai_api_key_here

# Flask Secret Key - Generate with: python -c "import secrets; print(secrets.token_hex(32))"
FLASK_SECRET_KEY=your_super_secure_flask_secret_key_for_production_here

# JWT Secret - Generate with: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=your_super_secure_jwt_secret_key_for_production_here

# =====================================
# 📧 Email Configuration (Password Recovery)
# =====================================

# Email Server Configuration
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USE_SSL=False
MAIL_USERNAME=your-production-email@gmail.com
MAIL_PASSWORD=your-production-app-password
MAIL_DEFAULT_SENDER=your-production-email@gmail.com

# Alternative Email Providers:
# Gmail: smtp.gmail.com:587 (TLS) - requires App Password
# Outlook: smtp-mail.outlook.com:587 (TLS)
# Yahoo: smtp.mail.yahoo.com:587 (TLS)
# SendGrid: smtp.sendgrid.net:587 (TLS)
# Mailgun: smtp.mailgun.org:587 (TLS)

# =====================================
# 🔐 Password Recovery Security
# =====================================

# Token Configuration
PASSWORD_RESET_TOKEN_EXPIRY_HOURS=24
PASSWORD_RESET_TOKEN_LENGTH=64

# Rate Limiting (per hour)
PASSWORD_RESET_RATE_LIMIT_USER=5    # per user email
PASSWORD_RESET_RATE_LIMIT_IP=10     # per IP address

# Security Settings
PASSWORD_RESET_CLEANUP_INTERVAL_HOURS=6    # cleanup expired tokens
PASSWORD_RESET_MAX_REQUESTS_PER_DAY=20     # global daily limit per IP

# Password Requirements
PASSWORD_MIN_LENGTH=8
PASSWORD_REQUIRE_UPPERCASE=True
PASSWORD_REQUIRE_LOWERCASE=True
PASSWORD_REQUIRE_NUMBERS=True
PASSWORD_REQUIRE_SPECIAL_CHARS=True

# =====================================
# 🌐 Application URLs & Branding
# =====================================

# Frontend URL (for email links)
FRONTEND_URL=https://your-production-domain.com
APP_NAME=Resume Editor
COMPANY_NAME=Your Company Name
SUPPORT_EMAIL=support@your-domain.com

# Email Template Configuration
EMAIL_LOGO_URL=https://your-domain.com/logo.png
EMAIL_FOOTER_TEXT=© 2024 Your Company. All rights reserved.
EMAIL_UNSUBSCRIBE_URL=https://your-domain.com/unsubscribe

# =====================================
# 🗄️ Database Configuration
# =====================================

# Database URL - Will be automatically set by most platforms
# PostgreSQL format: postgresql://username:password@host:port/database
# MySQL format: mysql+pymysql://username:password@host:port/database
DATABASE_URL=postgresql://user:password@host:port/resume_app

# =====================================
# 🔐 Google OAuth Configuration
# =====================================

# Google OAuth Credentials - Get from https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your_production_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_production_google_client_secret

# Google Redirect URI - Update with your production domain
GOOGLE_REDIRECT_URI=https://your-production-domain.com/auth/google/callback

# =====================================
# 🌐 Flask Application Configuration
# =====================================

# Flask Environment
FLASK_APP=app.server
FLASK_ENV=production
FLASK_DEBUG=0

# Server Configuration
PORT=5001
HOST=0.0.0.0

# =====================================
# 📊 Database Pool Configuration
# =====================================

# Database connection pool settings for production
DB_POOL_SIZE=10
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=3600
DB_POOL_PRE_PING=1

# =====================================
# 🔒 Security Configuration
# =====================================

# CORS Configuration
CORS_ORIGINS=https://your-frontend-domain.com,https://your-production-domain.com

# Session Configuration
SESSION_COOKIE_SECURE=1
SESSION_COOKIE_HTTPONLY=1
SESSION_COOKIE_SAMESITE=Lax

# Security Headers
FORCE_HTTPS=1

# =====================================
# 📝 Logging Configuration
# =====================================

# Log Level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Log Format
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s

# =====================================
# ⚡ Performance Configuration
# =====================================

# Maximum file upload size (in bytes)
MAX_CONTENT_LENGTH=16777216  # 16MB

# Request timeout (in seconds)
REQUEST_TIMEOUT=300

# =====================================
# Platform-Specific Variables
# =====================================

# Railway
# RAILWAY_STATIC_URL will be automatically set
# RAILWAY_ENVIRONMENT will be automatically set

# Heroku
# PORT will be automatically set by Heroku
# DATABASE_URL will be automatically set by Heroku Postgres

# Render  
# RENDER will be automatically set to true

# DigitalOcean App Platform
# APP_URL will be automatically set

# AWS
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your_aws_access_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key

# Google Cloud Platform
# GOOGLE_CLOUD_PROJECT=your-project-id
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# =====================================
# 🔧 Optional Features
# =====================================

# Redis (for caching, sessions, etc.)
# REDIS_URL=redis://user:password@host:port/0

# Email Configuration (for notifications)
# MAIL_SERVER=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USE_TLS=1
# MAIL_USERNAME=your-email@gmail.com
# MAIL_PASSWORD=your-app-specific-password

# File Storage (AWS S3, Google Cloud Storage, etc.)
# AWS_S3_BUCKET=your-s3-bucket-name
# AWS_S3_REGION=us-east-1

# Monitoring & Analytics
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
# GOOGLE_ANALYTICS_ID=GA-XXXXXXXXX

# =====================================
# 🚨 Important Security Notes
# =====================================

# 1. Never commit this file to version control with real values
# 2. Use your platform's secret management system when possible
# 3. Rotate secrets regularly
# 4. Use different secrets for different environments
# 5. Monitor for secret leaks in logs and error messages
# 6. Use strong, randomly generated secrets
# 7. Enable two-factor authentication on all service accounts

# =====================================
# 🛠️ Environment-Specific Overrides
# =====================================

# Staging Environment Overrides
# FLASK_ENV=staging
# FLASK_DEBUG=1
# LOG_LEVEL=DEBUG

# Development Environment Overrides  
# FLASK_ENV=development
# FLASK_DEBUG=1
# DATABASE_URL=postgresql://localhost/resume_app_dev